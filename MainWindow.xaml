<Window x:Class="DiscreteSimulation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiscreteSimulation"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Height="60" VerticalAlignment="Top">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <!-- Buttons in each column -->
                <Button x:Name="btnStart" Grid.Column="0" Content="Button 1" Margin="36,8" Click="ButtonClick"/>
                <Button x:Name="btnStop" Grid.Column="1" Content="Button 2" Margin="36,8" Click="ButtonClick"/>
                <ComboBox x:Name="cbStrategies" Grid.Column="2" Margin="36,16" SelectionChanged="SelectionChanged">
                    <ComboBoxItem x:Name="StartegyA" Content="Startegy A" />
                    <ComboBoxItem x:Name="StartegyB" Content="Startegy B" />
                    <ComboBoxItem x:Name="StartegyC" Content="Startegy C" />
                    <ComboBoxItem x:Name="StartegyD" Content="Startegy D" />
                    <ComboBoxItem x:Name="StartegyE" Content="Startegy E" />
                </ComboBox>
            </Grid>
        </GroupBox>

        <oxy:PlotView x:Name="plotView" Grid.Row="1" />
    </Grid>
</Window>
